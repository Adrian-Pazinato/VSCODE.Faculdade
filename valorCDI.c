#include <stdio.h>

int main () {
    int CDI, MercadoPago, ValorConta, ResultMes, ResultAno;
    printf("Digite o valor do CDI: ");
    scanf("%d", &CDI);
    printf("Digite o valor da conta: ");
    scanf("%d", &ValorConta);

    
}